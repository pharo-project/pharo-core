initialize-release
initializeWithRef: ref creation: cTime modification: mTime isDir: bool size: bytes posixPermissions: posixNumber 
	
	reference := ref.
	creation := cTime.
	modification := mTime.
	isDirectory := bool.
	size := bytes.
	posixPermissions := posixNumber