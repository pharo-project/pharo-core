scopeForContext: aContext	"return the variable scope of the context"	| blockSequenceStart ir irInstr |		(ir  := methodNode ir) generate.	irInstr := aContext isExecutingBlock		ifTrue: [ blockSequenceStart := aContext startpc + aContext numTemps - aContext closure numCopiedValues -  aContext closure numArgs.				 ir blockInstructionForPC: blockSequenceStart. ]		ifFalse: [ ir].	^irInstr sourceNode scope