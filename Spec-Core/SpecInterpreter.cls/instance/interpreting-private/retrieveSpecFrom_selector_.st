retrieveSpecFrom: anArray selector: aSelector	((self model respondsTo: #needRebuild) and: [ self model needRebuild not and: [ self model spec notNil ] ])		ifTrue: [			spec := self model spec.			self model needRebuild: true.			spec isRedrawable				ifTrue: [ spec removeSubWidgets ].			spec isBasic				ifTrue: [ ^ spec instance ] ]		ifFalse: [ spec := self computeSpecFrom: anArray first selector: aSelector ].		spec selector: aSelector.			^ nil