valueWithInterval: aDelay	"Executes the block every x milliseconds specified in arguments. Answers the continue boolean holder, so that you can set its contents to false to stop the valueWithInterval loop"		| continue |		continue := true asValueHolder.		[ [ continue contents ] whileTrue: [ 		self value.		aDelay wait. ] ] forkAt: Processor userBackgroundPriority.	^ continue