enableSearch
	"Enables search (this is the default option). 
	 While I could use different search functions (like a 'filtering' search), for now I 
	 just have this one"
	search := FTSearchFunction table: self