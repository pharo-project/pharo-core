runToSelection: selectionInterval inContext: selectedContext 	"Attempt to step over instructions in selectedContext until the 	execution reaches the selected instruction. This happens when the	program counter passes the begining of selectionInterval.		A not nill and valid interval is expected." 		(self pcRangeForContext: selectedContext) first >= selectionInterval first 		ifTrue: [ ^self ].	self stepOver: selectedContext.	[ selectedContext == self context and: [ (self pcRangeForContext: selectedContext) first < selectionInterval first ] ] 		whileTrue: [ self stepOver: selectedContext ]