blockInstructionForPC: aPC	"return the IRPushClosureCopy instruction whose body starts at aPC	 Note: caller needs to take care about the pushNils for temps "	| pc |	pc := aPC.	4 timesRepeat: [ 		| result  |		result := self firstInstructionMatching: [:ir | ir isPushClosureCopy and: [ir blockSequence first bytecodeOffset = pc ]].		result ifNotNil: [^result].		pc := pc + 1.		].